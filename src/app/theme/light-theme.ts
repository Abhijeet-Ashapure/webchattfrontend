import { Theme } from './symbols';

export const lightTheme: Theme = {
  name: 'light',
  properties: {
    '--background': '#1c9dea',
    '--on-background': '#ffffff',
    '--primary': '#fff',
    '--on-primary': '#fff'
  }
};
